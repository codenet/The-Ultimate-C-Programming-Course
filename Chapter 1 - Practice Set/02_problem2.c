#include <stdio.h>

int main(){
    int r = 6;
    int height = 10;
    printf("The area of circle with radius %d is %f\n", r, /* ?? */);
    printf("The volume of cylinder with radius %d and height %d is %f", r, height, /* ?? */);
    return 0;
}