#include <stdio.h>

int main()
{
    int n;
    int not_prime = 0;
    // Input n, Find if it is not prime.
    
    if (not_prime)
    {
        printf("%d is not prime\n", n);
    }
    else
    {
        printf("%d is prime\n", n);
    }

    return 0;
}