#include <stdio.h>

int main(){
    int year; 
    printf("Enter year: \n");
    scanf("%d", &year);

    // Either print "This is a leap year" or "This is not a leap year"
    // depending on the input year

    return 0;
}